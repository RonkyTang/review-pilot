HOST=127.0.0.1
PORT=4173

# Optional: store runtime data outside the source directory.
# REVIEWPILOT_DATA_DIR=/path/to/persistent/reviewpilot-data

# Optional: provide a stable 32-byte Base64 or 64-character hex key.
# Keep the real value private and never commit it.
# REVIEWPILOT_MASTER_KEY=base64-or-64-character-hex-key
